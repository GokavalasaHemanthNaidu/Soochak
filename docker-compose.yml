version: '3.8'

services:
  soochak:
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./ml_artifacts:/app/ml_artifacts:ro
    restart: unless-stopped
